/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.controlf5.co.in', 'controlf5.co.in'], // Ensure both domains are included
    },
};

export default nextConfig;
